invariants:
  - id: SEC-001
    statement: "No live, reachable secret exists in source, artifacts, or VCS history."
    class: secret-exposure
    falsify: "Scan (incl. git history + built artifacts) for key formats/high entropy; confirm a hit is live AND reachable."
  - id: SEC-002
    statement: "Security-sensitive crypto uses strong primitives, unique keys/IVs, and a CSPRNG."
    class: weak-crypto
    falsify: "Find MD5/SHA1-for-security, ECB, static/hardcoded key or IV, or rand() for tokens; then demonstrate the consequence."
